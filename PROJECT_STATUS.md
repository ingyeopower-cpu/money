# 고석현의 돈통 프로젝트 현황 및 작업 일지

**최종 업데이트:** 2026-09-15 22:35  
**프로젝트 경로:** `C:\Users\PA\Desktop\윤지의돈창고`  
**현재 배포 버전:** GitHub Pages 최신 빌드 (Commit: `863fd7d`)

---

## 1. 🌐 공식 서비스 및 핵심 링크 모음

| 항목 | 주소 / ID | 비고 |
| :--- | :--- | :--- |
| **공식 웹앱 (GitHub Pages)** | [https://ingyeopower-cpu.github.io/money/](https://ingyeopower-cpu.github.io/money/) | **구글 상단 배너 없는 메인 실서비스 주소** |
| **GitHub 소스코드 저장소** | [https://github.com/ingyeopower-cpu/money](https://github.com/ingyeopower-cpu/money) | `main` 브랜치 (모든 코드 수정 및 자동 배포 전담) |
| **백엔드 API (Google Apps Script)** | `https://script.google.com/macros/s/AKfycbwvmGOCVuYW6Uul4DZBe13JVEHZ0ie3BiOP6vDc-ND9k-E-yLbVgqohcjsNowLV7MldHg/exec` | 버전 5 활성 배포 (DB & 부부 동기화 전담) |
| **구글 스프레드시트 (클라우드 DB)** | `19FQSbeUfF_A8UbVxeXuMUckYiMkrZVjFJv5i1cYq1Y5vWPLUjV2d6W9i` | 자산 및 저축 데이터 영구 저장소 |
| **PC 바탕화면 바로가기** | `고석현의 돈통` (Windows 바로가기 `.lnk`) | 더블클릭 시 GitHub Pages 웹앱 즉시 실행 |

---

## 2. 🏛️ 시스템 아키텍처 및 배포/운영 원칙

- **프론트엔드 & 배포 전담 (GitHub 중심):**
  - 화면 UI/CSS, 버튼, 계산 로직 등 모든 기능 수정은 **GitHub (`main` 브랜치)를 통해 전담 관리**합니다.
  - 코드 변경 후 `git push origin main`을 실행하면 **30초 내에 모든 기기(아이폰, 갤럭시, PC)에 자동 배포**됩니다.
  - 구글 스크립트 에디터에서 배포 관리를 누르는 번거로운 과정이 불필요합니다.
- **백엔드 & 데이터베이스 전담 (Google 스프레드시트 / Apps Script):**
  - 구글 스프레드시트와 Apps Script는 화면을 직접 띄우지 않고, **보이지 않는 클라우드 데이터베이스(DB) 및 백엔드 API 역할만 수행**합니다.
  - 부부 실시간 데이터 저장/동기화(`doPost`, `doGet`) 및 주식 시세 크롤링(`refreshMarket`) 연동을 안정적으로 전담합니다.
  - 백엔드 스크립트(`code.gs`)를 수정할 경우에만 기존 배포 ID를 유지하며 **새 버전**으로 갱신합니다.

---

## 3. 💰 현재 자산 데이터 및 동기화 현황 (2026-09-15 기준)

- **현재 준비된 총자산:** **2억 1,749만원** (`217,492,171원`, 우리사주 800만원 포함)
- **입주 잔금 가용자산:** **2억 949만원** (`209,492,171원`, 우리사주 800만원 안전 제외)
- **최종 필요 잔금:** **2억 8,219만원** (입주 잔금 목표 달성률 **74.2%**, 잔여 잔금 **7,270만원**)
- **잔금 납부 D-day:** **D-600일** (2028년 05월 목표 기준, 약 20개월 잔여)
- **부부 자산 포트폴리오 구성:**
  - 👩 **아내 주식/ETF (8종목):** **8,963만원** (41.2%)
  - 👨 **남편 주식/우리사주 (4종목):** **3,083만원** (14.2%, 현대자동차 우리사주 800만원 포함)
  - 👫 **부부 공동 자금 (예적금/채권/금):** **9,703만원** (44.6%)
- **이번 달 저축 현황 (2026년 9월):**
  - 누적 저축액 **1,152만원** / 월 목표 **400만원** (`🎉 목표 288% 달성!`)
- **환율 현황:**
  - 원·달러 환율 **1,342.53원** (안정적 보존 및 자동 실시간 연동)

---

## 4. 🛠️ 최근 핵심 개발 및 개선 작업 내역

### ① PC 와이드 화면(Desktop) 가독성 및 UI 레이아웃 전면 개선
* 미디어 쿼리 닫는 괄호 복구 및 760px 데스크톱 최적화 컨테이너 신설.
* 상단 고정 탭 메뉴 바, 테이블 가독성 향상 및 두꺼비 모달 데스크톱 중앙 팝업 배치.

### ② 자산 감소 시 상승 축배(+218만원) 팝업 버그 해결 및 수치 동기화
* 실제 자산 변동액 부호(`diff < 0`)에 따라 상승/하락 모드가 100% 자동 강제 일치되도록 로직 수정.
* 하드코딩된 더미 수치를 제거하여 실제 계좌 변동과 정확하게 일치하도록 구현.

### ③ 두꺼비 모달 내 '주요 변동 요인 Top 3' 및 '어제 대비 환율 비교 카드' 신설
* 복수 계좌 자동 합산 Top 3 랭킹 리스트 및 환율 등락 비교 카드 렌더링.
* 1위 변동 종목에 맞춘 두꺼비 실시간 코멘터리 동기화.

### ④ 통화 표기 헬퍼(`moneyText`) 및 단위 표기 정상화
* `kfmt`와 `moneyText` 이원화로 '만원원' 중복 표기 원천 박멸.

### ⑤ 모바일 환율 및 주가 시세 갱신 속도/안정성 개선
* 다계층 조회 체계 확립 및 타임아웃/로딩 피드백 최적화(5~6초대 단축).

### ⑥ 주가 및 환율 갱신 시 전일 기준가(prevPrice, prevUsdKrw) 자동 보존 체계 구축
* 날짜 변경 시점 기준가 아카이빙 로직 적용.

---

### ⑦ 남편 현대자동차 우리사주(800만원) 항목 추가 및 잔금 가용자산 분리 연산 구현 (2026-09-15)
* **요청 사항:**
  - 주식 항목에 남편 우리사주(현대자동차) 고정금액 800만원 추가.
  - 총자산에는 정상 포함하되, 입주 잔금 치룰 때 쓸 수 없는 자산이므로 잔금 달성률 및 필요 저축액 산출에서는 제외되도록 분리 요청.
* **구현 내용:**
  1. **우리사주 계좌 타입 신설 및 현대자동차 등록:**
     - 계좌 목록에 `우리사주` 옵션 정식 추가 (`일반계좌`, `ISA`, `우리사주`).
     - 종목명 `현대자동차 (우리사주)`, 종목코드 `우리사주`, 고정 평가금액 **800만원** 등록 완료.
     - 시세 갱신(`syncPrices`, `refreshMarket_`) 시 우리사주/고정 종목은 시세 덮어쓰기에서 자동 제외되어 800만원이 항상 안전하게 보존됨.
  2. **잔금 가용자산(`usableAssets`) 분리 계산:**
     - `totalAssets()`(총자산): 우리사주 800만원 포함하여 부부 총자산 및 남편 자산에 정상 합산.
     - `lockedAssets()`: 우리사주 등 인출제한 자산(800만원) 집계.
     - `usableAssets()`: 실제 잔금에 쓸 수 있는 가용자산 산출 (`총자산 - 800만원`).
     - 잔금 D-day 카드, 부족액(`gap`), 만기 예상액(`projected`), 매월 필요 저축액(`need`)을 모두 **가용자산 기준**으로 정확하게 연산.
  3. **대시보드 UI 시인성 강화:**
     - Hero 카드에 `현재 준비된 총자산 2억 1,749만원 (잔금 가용: 2억 949만원)` 및 `우리사주 800만원 제외 안내` 상세 표시.
     - 부부 포트폴리오에 남편 자산 `주식/우리사주 4종목` 정상 표기.
     - 보유종목 테이블에 `🔒 잔금제외 자산` 배지 부착.
  4. **클라우드 DB(Google Sheet) 실시간 즉시 반영:**
     - 스프레드시트 실시간 DB에 현대자동차 우리사주 800만원 데이터 주입 완료.

---

### ⑧ 전 주식 종목 거래소 공시 전일 종가(`prevPrice`) API 직접 추출 체계 구축 및 두꺼비 등락 오류 원천 차단 (2026-09-15)
* **문제점:**
  - 당일 주식 시장에서 실제로 상승 마감한 종목(예: TIGER 미국배당다우존스)이 두꺼비 보기 모달에서는 전일 대비 하락한 것으로 표시되는 현상 발생.
* **원인 규명:**
  - 기존 방식은 시세 갱신 시 API에서 '현재가(`price`)'만 받아오고, '전일 종가(`prevPrice`)'는 과거 앱/DB에 저장되어 있던 직전 가격에 의존함.
  - 며칠 동안 시세 조회가 누락되었거나 날짜 동기화 시점이 어긋나면 전일 종가가 며칠 전 가격으로 고정되어, 실제 오늘 상승했음에도 과거 시세 대비 하락으로 잘못 판정되는 구조적 취약점 발견.
* **해결 및 영구 방어 조치 (전 종목 확대 적용):**
  1. **국내 상장 ETF 및 개별주 전수 적용 (네이버 금융 공식 API):**
     - 네이버 금융 실시간 API의 `compareToPreviousClosePriceRaw`(거래소 공시 전일대비 등락폭)와 `compareToPreviousPrice`(등락 방향)를 읽어와, **공시 전일 종가를 1원 단위까지 실시간으로 직접 산출하여 `prevPrice`에 매핑**.
  2. **해외/미국 주식 전수 적용 (야후 파이낸스 공식 API):**
     - SOXL 등 해외 직투 종목에 대해 야후 파이낸스 공식 메타데이터의 거래소 마감 전일 종가(`meta.chartPreviousClose`)를 직접 추출하여 매핑.
  3. **백엔드(Apps Script) & 프론트엔드(웹앱) 동시 적용:**
     - `index.html`과 `code.gs` 양쪽 모두 시세 갱신 시 `{ price, prevPrice }` 객체 단위로 저장하도록 전면 개편.
  4. **라이브 DB 전수 보정 및 배포:**
     - 구글 스프레드시트 라이브 DB에 저장된 12개 보유 종목 전수의 현재가와 전일 종가를 오늘 실제 장 마감 시세로 즉시 보정 완료 및 GitHub Pages 배포 완료.

---

### ⑨ 기 구현 완료된 위젯 및 기능 항목 정리
* **배당금 및 수익 기여도 연산 기능:** 보유 주식별 평가손익 및 부부 자산 기여도 분석 완료.
* **자산 추이 및 D-day 동기화 기능:** 잔금 D-day 기준 매월 목표 저축액 및 만기 시점 예상 가용자산 시뮬레이션 연동 완료.

---

### ⑩ 대시보드 원·달러 환율 및 자산 변동 기록 일간 시계열 주식 차트 구축 (2026-09-16)
* **요청 사항:**
  - 대시보드 원·달러 환율 위젯 하단에 일간 시계열 차트(주식 차트 형태의 꺾은선 그래프) 신설.
  - 가로축 시간 범위를 `1주일`, `1개월`, `분기(3개월)`, `1년`으로 자유롭게 전환 조회 가능하도록 구현.
  - 자산 변동 기록(시계열) 영역에도 동일하게 주식형 꺾은선 시계열 차트 구축 (`1주일`, `1개월`, `분기`, `1년`, `전체`).
* **구현 내용:**
  1. **원·달러 환율 일간 시계열 주식 차트:**
     - `liveFxCard` 하단에 토스/네이버증권 스타일의 유려한 주식형 꺾은선 차트 및 반투명 그라데이션 영역 채움 탑재.
     - 기간 필터 탭(`1주일`, `1개월`, `분기`, `1년`) 제공 및 기간별 최고/최저 환율과 기간 등락폭(원/%) 실시간 연산 HUD.
     - 오프라인/최초 로딩 시 0초 즉각 렌더링을 보장하는 베이스라인 데이터 탑재 + 백그라운드 Frankfurter 공식 API(1년 일간 환율) 자동 동기화 및 `localStorage` 캐싱.
     - 오늘의 실시간 고시 환율(`S.fx.usdKrw`)을 최신 데이터 포인트로 자동 연결하여 상단 숫자와 100% 일치.
     - 모바일 터치 및 마우스 스크럽(Scrub) 지원: 차트 위를 드래그하면 수직 크로스헤어 점선, 하이라이트 발광 마커, 일자/환율/변동폭 플로팅 툴팁이 부드럽게 실시간 추적.
  2. **자산 변동 기록 일간 시계열 주식 차트:**
     - '자산 변동 기록 (시계열)' 영역 상단에 전용 주식형 꺾은선 차트 신설.
     - 기간 필터 탭(`1주일`, `1개월`, `분기`, `1년`, `전체`) 지원.
     - `S.history` 과거 기록과 오늘 현재 총자산(`totalAssets()`)을 시간 순으로 정렬하여 자산 우상향 성장 추이를 주식 그래프로 시각화.
     - 상승 시 열정적인 레드/코랄(`#f04452`), 하락 시 쿨 블루(`#3182f6`)로 다이내믹 컬러 테마 적용.
     - 모바일 터치/마우스 스크럽 지원: 터치 시 해당 날짜의 총자산(억/만원 및 원 단위)과 기간 시작 대비 변동액을 툴팁에 표시.
  3. **UI/UX & 다크모드 최적화:**
     - Pretendard 폰트 및 모던 핀테크 디자인 시스템에 완벽하게 일치하는 반응형 SVG 벡터 렌더링.
     - 라이트/다크 테마 지원 및 모바일(360px~430px), 데스크톱(760px) 자동 리사이즈 대응.

---

### ⑪ 자산 상승 모달 두꺼비 에셋 3D 애니메이션 전면 개편 및 황금비 점프 모션 탑재 (2026-09-18)
* **요청 사항:**
  - 기존 실사 와인잔 두꺼비 이미지를 귀여운 3D 애니메이션 스타일의 두꺼비 캐릭터로 교체.
  - 두꺼비가 연잎 위에서 기쁨으로 폴짝폴짝 뛰어오르고, 하늘에서 황금(금화/엽전)이 비처럼 쏟아지는 동영상 느낌의 모션 구현.
* **구현 내용:**
  1. **고품질 3D 시네마틱 두꺼비 에셋 생성 (`toad_assets/`):**
     - 디즈니/픽사 스타일의 표정이 풍부하고 사랑스러운 3D 두꺼비 캐릭터 제작.
  2. **동적 물리 점프 & 황금비 쏟아지는 애니메이션 (`assets/toad_gain.gif` & `.webp`):**
     - 애니메이션 12원칙(Squash & Stretch, 도약 포물선, 착지 충격 흡수)을 적용한 유려한 폴짝 점프 모션 구현.
     - 36개의 입체 회전 3D 황금 엽전/코인이 하늘에서 후두둑 쏟아져 내리는 모션 합성.
     - 반짝이는 30개의 황금빛 스파클(✦) 폭발 및 연잎 그림자 동적 반응 연출.
  3. **모달 UI & 대시보드 브리핑 카드 싱크로율 최적화:**
     - 브라우저 캐시 방지를 위한 `v=20260918_v4` 캐시 버스팅 적용.
     - 모달 박스 및 GIF 컨테이너에 골드 앤 에메랄드 발광 림 섀도우 효과 추가.
     - 텍스트 코멘터리를 "황금 두꺼비의 축복 · 자산 상승" 및 황금비 연출에 맞게 자연스럽게 통일.

---

### ⑫ 자산 변동 기록 (시계열) 테이블 최신 3개 기본 노출 및 더보기 접기 토글 탑재 (2026-09-18)
* **요청 사항:**
  - 시계열 로그 테이블이 수십 행으로 길어져 대시보드 스크롤이 과도해지는 문제 개선.
  - 최신 3개 기록만 기본으로 노출하고, 나머지는 깔끔하게 숨김 처리.
* **구현 내용:**
  1. **최신 3개 행 기본 노출 & 4번째 이후 숨김 처리:**
     - `renderHistory()` 렌더링 시 최신 3개 행(`idx < 3`)만 기본 노출하고, 이후 행은 `.hist-extra-row`로 자동 분리하여 기본 `display: none` 적용.
  2. **모던 토스 스타일 [더보기 / 접기] 토글 버튼 탑재:**
     - 테이블 바로 아래에 `▼ 지난 기록 더보기 (N개 더 있음)` 토글 버튼 신설.
     - 클릭 시 전체 과거 기록이 즉시 부드럽게 펼쳐지며 `▲ 기록 간략히 접기 (최신 3개만 보기)`로 전환.
     - 전체 행 수가 3개 이하이거나 기록이 없을 때는 더보기 버튼이 자동으로 숨겨지도록 반응형 연동.
  3. **다크모드 & 테마 완벽 대응:**
     - 라이트/다크 테마 칩 버튼 디자인 시스템 일치 및 모바일 터치 최적화.

---

### ⑬ 모달 렌더링 구문 오류 긴급 수정 (2026-09-18)
* **원인:** 두꺼비 모달 텍스트 치환 시 `if-else` 분기 블록의 `else {` 키워드가 누락되어 자바스크립트 구문 오류(`moverComment` 중복 선언) 발생 -> 전체 스크립트 실행 중단 현상 초래.
* **조치:** `node --check`를 통한 전체 스크립트 구문 문법 검증 완료 및 `else` 분기 복원. GitHub Pages 배포 완료.

---

## 5. ☀️ 향후 작업 계획 및 인수인계 가이드

- **배포 및 검증 상태:**
  - GitHub Pages (`https://ingyeopower-cpu.github.io/money/`)에 최신 코드 자동 배포 완료.
  - 환율 및 자산 변동 기록 일간 시계열 주식 차트 정상 작동 확인.
  - Scratchpad 등 불필요한 임시 파일 탭 자동 오픈 원천 차단 상태 유지 중.
- **향후 유지보수 중점 항목:**
  1. **실사용 중 예외 케이스 및 버그 검토:**
     - 주말/공휴일 등 장 마감 기간 및 미국 증시 개장/마감 시차에 따른 시세 갱신 데이터 정합성 모니터링.
     - 부부 간 다기기(모바일/PC) 동시 접속 시 구글 스프레드시트 실시간 동기화 안정성 지속 점검.
  2. **사용자 실사용 피드백 대응:**
     - 실사용 중 발생하는 UI 미세 조정이나 계산 로직 피드백 접수 시 즉시 대응.


